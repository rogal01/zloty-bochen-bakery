# Złoty Bochen PHP Variant

Small server-rendered PHP demo for clients who need a practical inquiry form on classic shared hosting.

## Run Locally

```bash
php -S localhost:8080
```

Open <http://localhost:8080>.

The form does not write to disk or send email. It sanitizes submitted values and renders a demo confirmation so the flow can be reviewed safely.
